{
  "name": "silver-stream-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "CLOUDFLARE_API_TOKEN=$CLOUDFLARE_API_TOKEN CLOUDFLARE_ACCOUNT_ID=$CLOUDFLARE_ACCOUNT_ID wrangler dev",
    "deploy": "CLOUDFLARE_API_TOKEN=$CLOUDFLARE_API_TOKEN CLOUDFLARE_ACCOUNT_ID=$CLOUDFLARE_ACCOUNT_ID wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:create": "wrangler d1 create silver-stream-db",
    "db:migrate": "wrangler d1 migrations apply silver-stream-db",
    "db:migrate:local": "wrangler d1 migrations apply silver-stream-db --local",
    "storage:create": "wrangler r2 bucket create silver-stream-storage"
  },
  "dependencies": {
    "@clerk/backend": "^1.28.3",
    "drizzle-orm": "^0.38.3",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "~5.9.3",
    "wrangler": "^4.111.0"
  }
}
